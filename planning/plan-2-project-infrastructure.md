# Plan 2: Project Infrastructure Setup

**Status:** Done ✓

Goal: Initialize the frontend application using Angular 21, with ngrx signal stores for state management.

1. Update Angular CLI and workspace packages to Angular 21.
2. Create the Angular app in the repo and ensure it targets Angular 21.
3. Install Angular Material and ngrx signal stores.
4. Set up frontend folders: `src/app/features`, `src/app/styles`.
5. Create the base app shell, routing, and landing page; do not add placeholder files if they have no content yet.